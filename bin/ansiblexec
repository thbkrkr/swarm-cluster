#!/bin/bash

ENV=$(basename $MACHINE_STORAGE_PATH)
INVENTORY=/ops/ansible/inventory/$ENV

echo '{
  "date":"'$(date +%s)'",
  "level":"info",
  "kind":"ansible",
  "cmd":"ansible all -i '$INVENTORY' -m command -a `'$@'`",
  "host":"all"
}'

ansible all -i $INVENTORY -m command -a "$1 $2 $3 $4 $5"